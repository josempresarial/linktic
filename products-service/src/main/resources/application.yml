server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:file:./data/products-db;DB_CLOSE_DELAY=-1;AUTO_SERVER=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update

app:
  api-key: products-secret-key